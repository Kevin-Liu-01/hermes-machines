# User Profile — Kevin Liu

Princeton CS '26, founding engineer at Dedalus Labs (YC S25, dedaluslabs.ai). Lives in NYC and Princeton. Works on agent infrastructure: MCP-aware SDK, multi-tenant auth, microVM sandboxes, the "machines" runtime this agent itself is hosted on.

## How Kevin works

Trunk-based, small PRs (<200 LOC), Conventional Commits, `git switch` over `git checkout`, `git worktree` for stacked work. Likes deletions over additions. Hates `any` and ternaries-as-control-flow. Strong taste for tokenized design systems (Reticle/Sigil UI is his project).

## Style preferences

Asks for terse answers. Prefers ASCII over Unicode lookalikes. No emoji in code/commits/docs. Function under 70 lines or split it. File under 500 lines or split it. Never narrates code with comments — comments explain non-obvious intent only.

## Stack

TypeScript everywhere except the host-agent (Rust). Next.js 15 App Router, Tailwind v4, shadcn-style hand-rolled primitives, Reticle design system. Supabase + Clerk. Python 3.14 for backend tooling. uv over pip.

## Current focus

Dedalus DCS (Dedalus Cloud Services): host-agent, dhv microVMs, controlplane, machines API, dashboard. Building the agent infrastructure layer that people will run on top of. Also: hermes-persistent (this repo), Sigil UI design system, recurring blog drafts about agent infra.

## People

Reports loosely to Windsor Nguyen (CTO, @windsornguyen). Works closely with Shengming Liang (PM, @AgentWings), Anny Zhou, Aryan Mahajan, Catherine Di, Manna Patiparnprechavut, Tsion Kergo. Personal wiki at `~/Documents/GitHub/my-wiki`.

## Communication

Will redirect aggressively if you're wasting tokens on hedging. Will tell you when you're wrong. Reads everything you output. Don't pretend to know — verify.
